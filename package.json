{
  "name": "itour-punta-cana",
  "version": "1.0.0",
  "description": "iTour Punta Cana - Tourism website for tours and activities in Punta Cana",
  "scripts": {
    "start": "browser-sync start --server --port=3000 --files='**/*.html, styles/**/*.css, scripts/**/*.js' --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
